# Start everything
pnpm dev

# Backend only
pnpm --filter api start:dev

# Frontend only
pnpm --filter web dev

# Build backend
pnpm --filter api build

# Prisma migration
pnpm --filter api exec prisma migrate dev

# Prisma Studio
pnpm --filter api exec prisma studio

Stop-Service -Name "postgresql*"
Get-Service -Name "postgresql*"

pnpm --filter api exec prisma migrate dev --name update_projects
pnpm --filter api exec  prisma generate  


pnpm --filter api exec nest g controller  tasks --no-spec



✅ Phase 1 — Authentication (Done)
✅ Register
✅ Login
✅ Refresh Token
✅ Logout
✅ JWT Guard
✅ Current User Decorator
✅ Roles Guard

Status: ✔ Complete

✅ Phase 2 — Workspace Management (Almost Done)
✅ Create workspace (during registration)
✅ Invite member
✅ Leave workspace
✅ Transfer ownership
✅ Remove member
✅ Update member role
✅ List workspace members
🚀 Phase 3 — Projects

This is where TimeSync starts becoming a real application.

User Story

As a workspace member, I can create projects.

You'll build:

Project
---------
id
name
description
status
workspaceId
createdBy
createdAt
updatedAt

Endpoints:

✅POST   /workspaces/:workspaceId/projects  create project
✅GET    /workspaces/:workspaceId/projects  get projects
✅GET    /projects/:id
✅PATCH  /projects/:id
✅DELETE /projects/:id

Things you'll practice:

Relations
Pagination
Filtering
Authorization


🚀 Phase 4 — Tasks

Now it becomes similar to Jira or ClickUp.

Task
---------
title
description
priority
status
assignee
projectId
dueDate

Endpoints:

✅Create task
Assign task
Update task
Delete task
Move status

This introduces more business logic.

🚀 Phase 5 — Comments
POST /tasks/:id/comments
GET  /tasks/:id/comments
DELETE

You'll learn nested resources.

🚀 Phase 6 — Notifications

When someone:

invites you
assigns you a task
comments on your task

Create a notification.

You'll learn:

Events
Background jobs
RabbitMQ (later)
🚀 Phase 7 — File Uploads

Attach files to tasks.

Learn:

Multer
Cloudinary/S3
Validation
🚀 Phase 8 — Email

Send emails for:

Invitation
Password reset
Workspace invite
🚀 Phase 9 — Testing

You'll learn:

Unit tests
Integration tests
E2E tests
🚀 Phase 10 — Production

Deploy:

API
PostgreSQL
Redis
Frontend

CI/CD with GitHub Actions.





Before adding more features, I'd move to hardening the API:

Workspace selection / switching
    User can belong to multiple workspaces.
    Generate a new token when switching workspace context.
Authorization cleanup
    Make sure every endpoint checks:
    user → workspace membership → role → resource
Validation
    Make sure DTOs reject invalid UUIDs, dates, enums, empty strings, etc.
Slug handling
    Handle duplicate workspace/project slugs cleanly.
Soft-delete behavior
    Ensure deleted projects/tasks never appear in normal queries.
Global error handling
    Consistent response format for 400, 401, 403, 404, 409, etc.
Refresh-token security
    Test expired refresh tokens, invalid refresh tokens, logout, and token rotation.
API documentation
    Add Swagger/OpenAPI so you can test the whole backend easily.
Automated tests
    Start with authentication, workspace authorization, projects, and tasks.